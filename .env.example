# Copy to .env and fill in values
BAILEYS_AUTH_DIR=/data/auth
BAILEYS_BEARER=your-secret-dispatch-token        # must match WHATSAPP_DISPATCH_TOKEN in Lovable
ADMIN_BEARER=your-secret-admin-token             # for GET /qr and POST /session/logout
PORT=8080
